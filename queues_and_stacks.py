#general code for queues and stacks

class queue():

    def __init__(self):

    #FIFO
    #enqueue(item)
    #dequeue()
    #peek() #see what is at the front of the queue
    #isempty()

class stack():

    def __init__(self):

    #LIFO
    #push(item)
    #pop()
    #peek() - see what is at the top of the stack
    #is_empty()
