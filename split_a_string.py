

def split(astring, splitter):
    """Split a string by splitter and return list of splits."""

    word_list = []
    for char in astring:
        #iterate through chars,
        for char in astring:
        #concatentate chars until we reach the split word
        
        #append concatenated word
        
        #skip over split word
        
        #start process again